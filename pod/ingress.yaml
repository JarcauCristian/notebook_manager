apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name:
  namespace: cjarcau
spec:
  rules:
  - host: equipped-woodcock-needlessly.ngrok-free.app
    http:
      paths:
      - path:
        pathType: Prefix
        backend:
          service:
            name:
            port:
              number:
  ingressClassName: ngrok
